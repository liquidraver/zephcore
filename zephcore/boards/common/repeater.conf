# Repeater-specific config
#
# Build with: -DEXTRA_CONF_FILE="boards/common/repeater.conf"
#
# Disable Zephyr's auto USB init - we do it ourselves with 1200 baud touch detection
# for entering UF2 bootloader mode (like Arduino's behavior).
CONFIG_CDC_ACM_SERIAL_INITIALIZE_AT_BOOT=n
